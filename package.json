{
  "name": "classic-ia",
  "private": true,
  "version": "0.0.0",
  "description": "Classic IA — sistema unificado RH/SST + CQ. Frontend estático deployado via Vercel.",
  "scripts": {
    "serve": "npx http-server -p 3000 -c-1 .",
    "test:smoke": "playwright test tests/smoke",
    "test:smoke:headed": "playwright test tests/smoke --headed",
    "test:smoke:ci": "playwright test tests/smoke --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
